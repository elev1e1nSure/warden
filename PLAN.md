# warden

автономный cli-агент по компу. строгий, лаконичный, делает что говорят.

---

## стек

| слой | технология |
|---|---|
| tui | python + textual |
| llm | ollama (qwen2.5:7b или qwen3:8b) |
| computer use | pyautogui, pillow (скрины) |
| браузер | playwright |
| терминал/файлы | subprocess, pathlib, shutil |
| llm клиент | ollama python sdk |

---

## архитектура

```
пользователь
    ↓ промт
tui (textual)
    ↓
agent loop
    ↓
llm (ollama) ← system prompt + история + тулзы
    ↓
tool dispatcher
    ↓
[screenshot] [mouse/keyboard] [browser] [terminal] [filesystem]
    ↓
результат → стриминг обратно в tui
```

агент работает в петле: получает задачу → думает → вызывает тул → смотрит результат → думает ещё → отвечает.

---

## структура проекта

```
warden/
├── main.py              # точка входа
├── tui/
│   ├── app.py           # textual app
│   ├── widgets.py       # кастомные виджеты
│   └── theme.css        # стили
├── agent/
│   ├── loop.py          # агентский луп
│   ├── tools.py         # все тулзы
│   └── prompts.py       # системный промт
├── PLAN.md
├── CLAUDE.md
└── AGENTS.md
```

---

## mvp scope

**входит в mvp:**
- [ ] textual tui: поле ввода внизу, лог сверху, стриминг ответа
- [ ] подключение к ollama, стриминг токенов
- [ ] одна петля: промт → llm → ответ
- [ ] тул: screenshot (скрин + описание что видит)
- [ ] тул: run_command (запуск команды в терминале)
- [ ] тул: type_text / move_click (базовое управление мышью/клавой)
- [ ] история диалога в рамках сессии
- [ ] красивый вывод: стриминг, выделение жирным, цвета без вырвиглаза

**не входит в mvp:**
- браузер (playwright) — следующий этап
- файловый менеджер ui
- сохранение истории между сессиями
- несколько агентов / мультитаскинг

---

## тех. детали реализации mvp (tui + чат без тулз)

### tui layout

- **Input** фиксирован внизу экрана — однострочное поле ввода.
- **RichLog** сверху — скроллируемый лог сообщений. стриминг пишем сюда посимвольно.
- **Footer** — статусная строка: "ollama: подключено / не подключено", имя модели.
- никаких панелей, рамок, кнопок. только текст + цвет + жирность.
- управление: `enter` — отправить, `esc` — очистить поле, `ctrl+c` — выход.

### автозапуск ollama

- при старте проверяем `ollama list` через subprocess. если код != 0 — запускаем `ollama serve` в фоне (subprocess.Popen, detached).
- ждём готовности: пингуем `ollama.list()` из sdk в цикле с таймаутом 30 сек.
- если не поднялось — показываем ошибку в tui и выходим.
- по умолчанию модель: `qwen3:8b`. если нет — пуллим (`ollama pull`) с индикатором в статусной строке.

### стриминг и чат

- история: `list[dict]` в памяти — `{"role": "user"|"assistant", "content": str}`.
- отправляем в ollama через `ollama.chat(model=..., messages=history, stream=True)`.
- токены приходят в генератор — пишем в RichLog через `rich.Text` с накоплением.
- асинхронно: `asyncio.create_task` для запроса к llm, tui остаётся отзывчивым.

### форматирование ответа

- markdown парсим через `rich.markdown.Markdown` — автоматом получаем bold, code blocks, lists.
- кастомные цвета через `theme.css` (textual) — cyan для имени ассистента, жёлтый для системных сообщений, red для ошибок, dim grey для метаинфо.
- разделение сообщений: имя + timestamp dim, потом тело.

### состояния

```
[idle] → user набирает → [sending] → стриминг ответа → [idle]
                    ↓ ошибка сети → [error] → [idle]
```

---

## план реализации mvp v1 — чат + tui + ollama

### ~~шаг 1: скелет проекта~~

- [x] создать `main.py` — точка входа, парсинг аргументов (`--model`, `--no-auto-ollama`).
- [x] создать `tui/app.py` — базовый ` textual.App`, подключить `theme.css`.
- [x] создать `tui/theme.css` — тёмная тема, кастомные цвета (cyan, yellow, red, dim).
- [x] создать `agent/ollama_client.py` — обёртка над `ollama` sdk: проверка, запуск, pull, chat.
- [x] `requirements.txt`: `textual`, `ollama`, `rich`.

### ~~шаг 2: базовый tui~~

- [x] layout: `Vertical` → `RichLog` (expand) + `Input` (dock bottom) + `Footer` (dock bottom).
- [x] `RichLog` — вывод системных сообщений, приветствие, статус.
- [x] `Input` — однострочный, `enter` → отправка, `esc` → очистка.
- [x] `Footer` — статус ollama, имя модели, подсказки клавиш.
- [x] никаких рамок, панелей, кнопок — только текст + цвет.

### ~~шаг 3: автоподнятие и закрытие ollama~~

- [x] при старте: `ollama_client.check()` — subprocess `ollama list`, таймаут 5 сек.
- [x] если не отвечает: `subprocess.Popen(["ollama", "serve"], creationflags=CREATE_NEW_PROCESS_GROUP)` (win) или `start_new_session=True` (unix).
- [x] пинг в цикле: `ollama.list()`, таймаут 30 сек, прогресс в Footer.
- [x] если не поднялось — ошибка в RichLog, exit(1).
- [x] если модели нет — `ollama.pull(model)` с индикатором.
- [x] при выходе (`ctrl+c` / закрытие) — убивать запущенный `ollama serve` (только если мы сами подняли).

### ~~шаг 4: чат с llm~~

- [x] история: `list[dict]` в `agent/chat.py` — `{"role": "user"|"assistant", "content": str}`.
- [x] отправка: `ollama.chat(model=..., messages=history, stream=True)` через `asyncio.to_thread` или `async for` если sdk поддерживает.
- [x] стриминг: токены накапливаем в строку, пишем в `RichLog` через `rich.Text` с авто-скроллом.
- [x] после завершения — финализируем сообщение, добавляем в историю.
- [x] состояния: `[idle]` → `[sending]` (блокируем input, показываем "...") → `[streaming]` → `[idle]`.

### ~~шаг 5: полировка~~

- [x] форматирование: `Markdown` для ответов ассистента, `Text` для пользователя.
- [x] цвета: cyan для "warden >", белый для текста, dim grey для timestamp, red для ошибок.
- [x] обработка ошибок: таймаут, ollama не найден, модель не скачалась — красный текст в лог.
- [x] scroll-to-bottom при новых сообщениях.
- [x] тест: запуск без ollama → ожидание auto-launch → отправка сообщения → стриминг ответа.

**итог mvp v1:** запускаешь `python main.py`, ollama поднимается сама, пишешь промт — получаешь стриминг ответа в минималистичном tui.

---

## визуальный стиль

- минимализм: текст, цвет, жирность
- цвета: белый для обычного текста, cyan для варден, жёлтый для тулзов, красный для ошибок, серый для метаинфо
- управление: стрелки, enter, esc, ctrl+c и другие клавиши при надобности
- без мышки в программе

---

## модель

**рекомендуется:** qwen3:8b (лучше тулзы, быстрее)

запуск: `ollama run qwen3:8b`
